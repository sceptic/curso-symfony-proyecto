{% extends '::base.html.twig' %}

{% block body -%}
    <h1>User edit</h1>

    {{ form(edit_form) }}
    {{ form(delete_form) }}
       
        <a href="{{ path('user') }}">
            Back to the list
        </a>
{% endblock %}
