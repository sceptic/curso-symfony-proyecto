{% extends 'AdminSettingsBundle::base.html.twig' %}
{% block adminHeading -%}SubCategory{% endblock %}
{% block admin %}
<table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ entity.category.name }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ entity.slug }}</td>
            </tr>
        </tbody>
    </table>
    {{ form(delete_form) }}
<hr/>
        <a  class="btn btn-default"  href="{{ path('subcategory') }}">
            Volver a la lista
        </a>
        <a class="btn btn-default"  href="{{ path('subcategory_edit', { 'id': entity.id }) }}">
            Editar
        </a>
{% endblock %}
